version: "2.1"
services:
  wireguard:
    image: lscr.io/linuxserver/wireguard:latest
    container_name: wireguard
    restart: unless-stopped

    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Zurich
      - SERVERURL={$wireguard_server_ip}
      - SERVERPORT=51820
      - PEERS=4
      - PEERDNS=${dns_server_ip}
      - dns_server_ip=${dns_server_ip}
    ports:
      - 51820:51820/udp
    volumes:
      - /lib/modules:/lib/modules
      - ${HOME}/docker_data/wireguard:/config
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1