version: "3"

services:
  pihole:
    container_name: pihole
    image: pihole/pihole:latest
    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "2080:80/tcp"
    environment:
      TZ: 'Europe/Zurich'
      WEBPASSWORD: '${USE_PIHOLE_PASSWD}'
      DNS1: 9.9.9.9
      DNS2: 8.8.8.8
    volumes:
      - '${NOW_DOCKER_DATA}/pihole/etc-pihole:/etc/pihole'
      - '${NOW_DOCKER_DATA}/pihole/etc-dnsmasq.d:/etc/dnsmasq.d'     
    restart: unless-stopped